struct Point {
    int x;
    int y;
}

component Broken {
    state<Point> origin = Point { x: 0, y: 0 };

    void run() {
        Point { x, z } = origin;
    }

    view {
        <p>"static"</p>
    }
}
